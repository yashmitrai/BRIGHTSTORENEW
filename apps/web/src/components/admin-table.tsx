import { Search } from "lucide-react";
import { Pill } from "./ui";
export function AdminTable({title,description,columns,rows}:{title:string;description:string;columns:string[];rows:string[][]}){return <><h1 className="font-display text-3xl font-extrabold">{title}</h1><p className="mt-1 text-sm text-slate-500">{description}</p><label className="mt-7 flex h-11 max-w-xl items-center gap-3 rounded-xl border bg-white px-4 dark:border-slate-700 dark:bg-slate-900"><Search size={16}/><input className="w-full bg-transparent text-sm outline-none" placeholder={`Search ${title.toLowerCase()}`}/></label><div className="mt-5 overflow-hidden rounded-2xl border bg-white dark:border-slate-800 dark:bg-slate-900"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-[10px] uppercase tracking-wider text-slate-400 dark:bg-slate-800"><tr>{columns.map(x=><th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={i} className="border-t dark:border-slate-800">{row.map((x,j)=><td key={j} className="px-5 py-4">{j===row.length-1?<Pill className={x.includes("Pending")||x.includes("Review")?"border-amber-200 bg-amber-50 text-amber-700":x.includes("Flag")?"border-rose-200 bg-rose-50 text-rose-700":""}>{x}</Pill>:x}</td>)}</tr>)}</tbody></table></div></div></>}
